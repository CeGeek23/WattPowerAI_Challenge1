# --- runtime artifacts (regenerated on every run - never commit) ---
__pycache__/
*.pyc
.venv/
venv/
model_state/
output*/
.val_out*/
.eval_out_*/
validation_report.txt

# real measurement data never belongs in this repo
/data/
/dataset/
/*102Ah_*/
*.rar

#
.DS_Store
# dataset fetched from the GitHub release, not from git
.dataset_cache/
# cache parquet de l analyse exploratoire (regenerable)
analysis/.cache/
