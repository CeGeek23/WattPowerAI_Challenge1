# ===========================================================================
#  Le SEUL fichier installe par les organisateurs, dans leur environnement.
#
#   uv pip install -r requirements.txt     synchroniser .venv
#   uv run python run_model.py --model train --input dataset --output-dir output
#   Ajouter un paquet = uv pip install <pkg>  PUIS ajouter la ligne ici.
#   (`uv venv` cree l'env, il ne l'active pas ; `uv run` rend l'activation
#    inutile. Sinon : source .venv/bin/activate.)
#
#  AVANT LA SOUMISSION : supprimer d'ici tout ce que my_model/ n'importe pas.
#  Leur machine est un Windows Server 2022 et leur version de Python n'est pas
#  publiee ; un paquet inutilise ne peut rien gagner et peut faire echouer
#  l'installation a l'intake.
# ===========================================================================

# --- socle (utilise par le framework lui-meme) ---
numpy>=1.24
pandas>=2.0

# --- modelisation ---
scipy>=1.10                # ajuste de courbes de fade, lissage, optimisation
scikit-learn>=1.3          # extrapolation entre points de fonctionnement, GPR
lightgbm>=4.0              # gradient boosting sur features par cycle
xgboost>=2.0               # alternative
statsmodels>=0.14          # regression, series temporelles, intervalles
optuna>=3.4                # recherche d'hyperparametres
joblib>=1.3                # parallelisation, cache disque des features

# --- exploration et visualisation ---
matplotlib>=3.7            # courbes SOH vs cycle, residus, ICA/dQ-dV
seaborn>=0.13              # comparaisons entre cellules/conditions
plotly>=5.18               # courbes interactives (zoom cycle, survol)
jupyterlab>=4.0            # notebooks d'analyse
ipykernel>=6.25            # noyau VS Code / Jupyter
ipywidgets>=8.1            # sliders pour explorer les cycles

# --- lecture des 559 Mo de CSV ---
pyarrow>=14.0              # moteur CSV rapide, cache en parquet
tqdm>=4.66                 # barres de progression

# --- qualite ---
pytest>=7.4
ruff>=0.1

# --- decommenter si vous partez sur du deep learning (~2.5 Go) ---
#   les poids pre-entraines se livrent DANS my_model/, aucun reseau pendant fit()
# torch>=2.1
